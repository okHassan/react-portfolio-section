const index = ({ children }) => {
  return (
    <h1 className="md:text-[39px] text-[#EDEDED] font-bold md:leading-[48px] font-['Montserrat'] title-underline">
      {children}
    </h1>
  );
};

export default index;
